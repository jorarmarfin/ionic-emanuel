import { LinkyoutubePipe } from './linkyoutube.pipe';

describe('LinkyoutubePipe', () => {
  it('create an instance', () => {
    const pipe = new LinkyoutubePipe();
    expect(pipe).toBeTruthy();
  });
});
